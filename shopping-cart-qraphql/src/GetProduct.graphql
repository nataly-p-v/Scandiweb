query GetProduct($id: String!) {
	getProduct(id: $id) {
      id
      name
      inStock
      gallery
      description
      category
      attributes {
        name
      }
      prices {
        amount
        currency{
          symbol
          label
        }
      }
	}
}